# Question Card — Demo (React + TypeScript + Vite)

Небольшое демо‑приложение с карточками вопросов (React, TypeScript, Vite, Tailwind, TipTap, KaTeX).

Опробовать приложение можно по ссылке (Vercel deployment): https://global-generation-test.vercel.app/

---

## Быстрый старт

**Требования**:

- Node.js >= 18 (рекомендуется)
- npm (или yarn / pnpm)

**Установка зависимостей**:

```bash
npm install
```

**Запуск в режиме разработки**:

```bash
npm run dev
```

**Сборка для продакшена**:

```bash
npm run build
```

**Локальный просмотр собранной версии**:

```bash
npm run preview
```

**Проверка линтером**:

```bash
npm run lint
```

---

## 🚧 Что внутри проекта

Ключевые технологии:

- Vite + React + TypeScript
- Tailwind CSS
- TipTap (rich text renderer)
- KaTeX для рендеринга математических формул

Основная структура (важные файлы/каталоги):

- `src/` — исходники приложения
  - `App.tsx`, `main.tsx` — точка входа
  - `components/QuestionCard/` — карточка вопроса и её подкомпоненты
    - `AnswerOptions.tsx` — варианты ответов (демо: баннер и блокировка выбора)
    - `Explanation.tsx` — объяснение: показывается после проверки; в демо режиме объяснение размывается
    - `TipTapRenderer.tsx` — рендерер TipTap + KaTeX

---

## ℹ️ Особенности демо‑режима

- У вопросов может быть флаг `isDemo`. В демо:
  - объяснение (`Explanation`) после проверки **заблюрено**, если включен демо-режим.

Это поведение реализовано в компоненте `Explanation`.

---

## Как добавить/редактировать вопрос

Вопросы представлены объектом типа `Question` (см. `src/components/QuestionCard/types.ts`). На практике нужно указать:

- `id` — уникальный идентификатор
- `stem` — TipTap документ (контент вопроса)
- `options` — массив вариантов `{ id, text }`
- `explanation` — строка (опционально)
- `isDemo` — булев флаг для демонстрации ограниченного доступа

---

## Отладка KaTeX

Если в тексте есть некорректные формулы, компонент `TipTapRenderer` перехватывает ошибку и вызывает `onKatexError` — можно использовать это для логирования или отображения уведомления.

---
